= simple_form_for [@tutorial, @video], :html => { :class => 'form-horizontal' } do |f|
  = f.input :title
  = f.input :description
  = f.input :video_url
  = f.input :image_url
  = f.button :submit, :class => 'btn-primary col-md-offset-2'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), tutorial_videos_path, :class => 'btn btn-default'
